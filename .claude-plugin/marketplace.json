{
  "$schema": "https://json.schemastore.org/claude-code-marketplace.json",
  "version": "1.0.0",
  "name": "claude-code-setup-optimizer",
  "description": "Dos plugins propios de David García Gordo para trabajar con Claude Code: working-methods (la columna /forge-run — align → draft + grill ×3 → spec → re-grill ×2 → plan → verify, con 2 checkpoints del dueño en lote; /grill, /handoff) y automations (la skill optimize-my-setup, hooks, /release, plantillas de permisos). Para la suite completa (+ forge-methodology, design-review, token-economy) usa el catálogo dedicado davidgarciagordo/claude-plugins.",
  "owner": {
    "name": "David García Gordo",
    "url": "https://github.com/davidgarciagordo"
  },
  "plugins": [
    {
      "name": "working-methods",
      "displayName": "Working Methods (David)",
      "description": "/forge-run — the Forge methodology with ENFORCEMENT: 12 phases whose order lives in code (forge.js), machine-checked gates, and a hook that blocks the PR until the run's artifacts exist. Plus /grill (adversarial ×3 on any artifact) and /handoff (session relay), both usable standalone.",
      "source": "./plugins/working-methods"
    },
    {
      "name": "automations",
      "displayName": "Setup Automations",
      "description": "Tailors a repo's entire Claude Code config (.claude: CLAUDE.md, settings.json permissions/hooks/env, skills, agents, workflows, .mcp.json, output-styles) via a deterministic scanner plus a mandatory multi-check gate — nothing is written until the user ticks it. Ships a fail-closed guard-append-only hook, /release, and templates (hooks, domain reviewer agents generated per repo, permissions allowlist). Language-agnostic.",
      "source": "./plugins/automations"
    }
  ]
}
